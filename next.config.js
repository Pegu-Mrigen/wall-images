/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["demo-ngo.vercel.app", "acb-gohpur.vercel.app"],
  },
};

module.exports = nextConfig;
